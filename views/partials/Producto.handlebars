<div class="col-12 col-md-4 d-flex justify-content-center">
    <div class="card shadow p-2 my-2" style="max-width: 15rem;">
        <img src="/{{nombre}}.png" alt="{{nombre}}" class="img-fluid">
        <h4>{{nombre}}</h4>
        <button type="button" class="btn btn-primary btnAbrirModal" data-nombre="{{nombre}}" data-imagen="/{{nombre}}.png" data-descripcion="{{descripcion}}">Ver Detalles</button>
    </div>
</div>